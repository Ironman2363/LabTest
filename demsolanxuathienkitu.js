let hoTen = "Pham Tien Dungn";
let chucandem = "n";
let dem = 0;
for (var i = 0; i < hoTen.length; i++) {
  if (hoTen[i] == chucandem) {
    dem++;
  }
}
console.log(dem);

//Viết một hàm nhận vào một chuỗi và một ký tự, sau đó trả về số lượng lần xuất hiện của ký tự đó trong chuỗi.
